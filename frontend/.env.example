# URL du front telle que le téléphone doit l’ouvrir (hotspot / Wi‑Fi). Pré-remplit le champ « URL pour les QR » dans l’admin.
# VITE_PUBLIC_FRONTEND_URL=http://172.20.10.2:5173

# Build production uniquement : URL du backend si différent du proxy /api
# VITE_API_BASE_URL=https://api.example.com
